/**
 * Generates an HTML title element.
 * @param {string} text - The text content for the title.
 * @returns {string} - The HTML string for the title element.
 */
export function Title(text) {
  return `<h1>${text}</h1>`;
}
